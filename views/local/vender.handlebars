<link rel="stylesheet" href="../modular/estilos/comprado.css">
<div class="comprado">
     <ul class="fondo-nav nav nav-tabs ">
        <li class="nav-item">
            <a class="nav-link active" href="/local/">Activación</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="/users/password">Cambiar contraseña</a>
        </li>
    </ul>
    <div class="row comprado">
        <div class="col-lg-4 col-md-4 "></div>
        <div class="col-lg-4 col-md-4 ">
            <div id="centralMensajes">
                {{#if success_msg}}<div class="alert alert-success">{{success_msg}}</div>{{/if}} 
                {{#if error_msg}}<div class="alert alert-danger">{{error_msg}}</div>{{/if}} 
                {{#if error}}<div class="alert alert-danger">{{error}}</div>{{/if}}
            </div><br>
            <div class="fondo-blanco">
                <h2 class="centrado">Aplicación de descuento</h2><br>
                <img class="profile-img" src="{{user.logotipo}}"  alt="">
                <form class="form-signin"  method="post" onsubmit="activar_desc(event)">
                    <div class="form-group">
                        <label for="">Código</label>
                        <input step="1" type="number" id="numero_act" step="1" min="0" class="form-control" required value="1">
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary">Aplicar descuento</button>
                    </div>
                </form>
            </div>           
        </div>
        <div class="col-lg-4 col-md-4 "></div>
    </div>
</div>
<style>
    .profile-img{
        width: 100%;
        margin: 0 auto 10px;
        display: block;
        -moz-border-radius: 10%;
        -webkit-border-radius: 10%;
        border-radius: 10%;
    }   
</style>
<script src="../modular/codigo/local.js"></script>
<div class="modal fade bd-example-modal-lg" id="modal_ventas" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Vender</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <h2 class="centrado">Seleccionar beneficio a usar</h2>
            <p>Cliente: <b id="cliente-compra"></b></p>
            <div class="centrado" >
                <div class="row" id="beneficios">
                    
                </div>
            </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cerrar</button>
      </div>
    </div>
  </div>
</div>